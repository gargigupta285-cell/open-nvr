# ============================================================
# OBSOLETE — this overlay was superseded by ISSUE-7 v3.
# ============================================================
#
# The Tier 0 default (`docker-compose.tier0.yml`) now uses the
# pre-baked yolov8-weights image directly, with a build fallback
# for hosts that can't pull from ghcr.io. There is nothing this
# overlay does that `docker-compose.tier0.yml` alone doesn't.
#
# If you have this file in your tree, you can safely run
#
#     git rm docker-compose.tier0.offline.yml
#
# and use the standard documented command on every host,
# filtered network or not:
#
#     docker compose -f docker-compose.tier0.yml up -d
#
# We keep this file as a no-op stub so any operator who pasted
# the old dual-flag command (`-f docker-compose.tier0.yml -f
# docker-compose.tier0.offline.yml up -d`) still sees the stack
# come up cleanly instead of getting a "file not found" error.
# ============================================================
services: {}
